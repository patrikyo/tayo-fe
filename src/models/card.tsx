class Card {
  constructor(public readonly img: any, public readonly title: any, public readonly body: any, public readonly button: any, public readonly icon: any, public readonly sectionId: any, public readonly size:any) {
  }
};

export default Card;

